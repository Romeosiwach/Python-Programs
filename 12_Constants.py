# Python doesn't have its built in constants type
# By convention python uses the variables those names starts with capital letters to define a costant

MY_NAME=700                       # This is a constant don't change  the value #
print(MY_NAME)

FILE_SIZE_LIMIT = 2000               # This is a constant don't change  the value #
print(FILE_SIZE_LIMIT)